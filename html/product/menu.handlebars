<nav class="menu">
  <section class="content">
    <section class="left">
      <img class="logo" src="https://www.hyper.sh/logo/logo_single_dark.svg">
      <div class="title-wrap">
        <div class="title">{{category}}</div>
        <div class="desc">{{desc}}</div>
      </div>
      {{#if region}}
      <div class="region-select-wrap">
        <select name="Region" class="region-select active">
          <option value="">Los Angeles</option>
          <option value="frankfurt">Frankfurt</option>
        </select>
      </div>
      {{/if}}
    </section>

    <section class="btns {{active}}">
      <a href="/{{category}}" class="btn btn-features">FEATURES</a>
      {{#unless noPrice}}
      <a href="/{{category}}/pricing.html" class="btn btn-pricing">PRICING</a>
      {{/unless}}
      <a href="https://docs.hyper.sh/{{category}}" class="btn btn-docs">DOCS</a>
    </section>
  </section>
</nav>


